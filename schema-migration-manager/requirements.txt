Flask==3.0.3
psycopg2-binary==2.9.10   # for PostgreSQL (remove if using MySQL)
python-dotenv==1.0.1     # optional (for storing DB credentials in .env file)
click==8.1.7
PyYAML==6.0.2
pytest==7.4.0
# Note: PyYAML may require a binary wheel on Windows. If pip attempts to
# build from source and fails, install a wheel matching your Python version
# or use a Python version with prebuilt wheels (e.g., 3.11), or install
# build tools (Visual Studio Build Tools) so the package can compile.
mysql-connector-python==8.0.33  # optional: required to use MySQL backend